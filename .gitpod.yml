image:
  file: Dockerfile
ports:
  - port: 3306
    onOpen: ignore
tasks:
  - name: MySQL
    init: >
        /etc/init.d/mysql start;
        mysql -e "create database chat_system";
        mysql < examples/chat_system.sql;
  - name: Welcome
    command: >
        python3 welcome.py;
